# Epidemiology Example

This folder contains the Live Script demonstration for SIR fitting.

- `SIR_fit_AL.mlx`

The associated reusable code lives in `src/epidemiology/`, and sample input data lives in `sample-data/sir_covid/`.
